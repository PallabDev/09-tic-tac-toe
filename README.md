# Tic Tac Toe

A full-stack Tic Tac Toe game with a React/Vite frontend and an Express, MongoDB, and Socket.IO backend.

## Run

```bash
docker compose up -d
cd backend && npm run dev
cd frontend && npm run dev
```
